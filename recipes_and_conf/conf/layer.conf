BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
	          ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-wp-module"
BBFILE_PATTERN_meta-wp-module = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-wp-module = "6"
